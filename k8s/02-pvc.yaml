apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dilek-kumbarasi-pvc
  namespace: dilek-kumbarasi
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
